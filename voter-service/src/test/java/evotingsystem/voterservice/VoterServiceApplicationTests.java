package evotingsystem.voterservice;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

/**
 * Tests de base pour l'application VoterService.
 * 
 * Ces tests vérifient que le contexte Spring Boot se charge correctement.
 * 
 * @author Équipe E-Voting System
 * @version 1.0.0
 */
@SpringBootTest
class VoterServiceApplicationTests {

    /**
     * Test pour vérifier que le contexte Spring Boot se charge correctement.
     */
    @Test
    void contextLoads() {
        // Ce test vérifie simplement que le contexte Spring Boot peut être chargé
        // sans erreur. Si cette méthode s'exécute sans exception, le test passe.
    }
}

